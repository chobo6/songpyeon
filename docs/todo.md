# TODO

> 지금까지 한 작업은 git 커밋 로그와 `docs/REQUIREMENTS.md`/`docs/ARCHITECTURE.md`/`docs/TROUBLESHOOTING.md` 참고. 이 문서는 앞으로 할 일만 정리.

## 다음에 먼저 볼 것

- [ ] **iOS에서 버튼 입력이 씹히는 현상 — 원인 특정, 다음 수정 방향 논의 중**: 안드로이드 문제(2026-07-15 이전 세션)는 해결됐지만 아이폰에서 재발. 시도 1~4(Pointer→Touch Events, 오디오 풀링, 리렌더 비용 축소, touch-action 확장) 상세는 `docs/TROUBLESHOOTING.md` #19 참고 — 전부 부분적이거나 효과 없었음. 실기기 터치 로그(임시 오버레이, 검증 끝나고 삭제함 — 재사용 설계는 `docs/TROUBLESHOOTING.md` #20에 기록)로 실제 데이터 확보한 결과: **서버가 턴 실패를 이미 확정했는데 클라이언트의 `disabled` 상태가 그걸 반영하기까지의 왕복 시간 동안 들어온 터치는, 서버(`handlePressButton`의 `if (this.turnDecided) return`)가 아무 신호 없이 조용히 무시함** — 터치는 정상 발생했는데 반응이 없어서 씹힘으로 느껴지는 것으로 추정. 신호 약한 환경(1칸)일수록 이 왕복 시간이 길어져 더 자주 겪음. **다음 단계**: "이미 늦은 입력"에 대해 서버가 클라이언트에 최소한의 신호라도 보내주는 방향, 혹은 같은 색이 비정상적으로 짧은 간격(예: 100ms 이내)으로 재입력되면 방어적으로 걸러내는 방향 논의 중. **새 단서**: 카카오톡 인앱브라우저로 실행하면 씹힘이 재현 안 됨 — iOS는 애플 정책상 모든 브라우저가 동일한 WebKit 엔진을 강제로 쓰므로 엔진 차이는 아니고, 사파리/크롬/네이버 같은 범용 브라우저 앱이 WKWebView 위에 얹는 자체 네이티브 제스처 인식기(엣지 스와이프 뒤로가기 등, 웹페이지가 통제 불가능한 레이어)가 원인일 가능성 — 근거는 정황 증거 수준, 확정 아님. 상세는 `docs/TROUBLESHOOTING.md` #19 참고. 별개로 발견된 "게임 중 시퀀스보드가 가끔 확대(줌인)되는" 현상은 시도 5(`html`에 `touch-action: manipulation` 전역 추가)로 1차 수정했으나 재검증 결과 오히려 악화(더블클릭만으로도 줌, 범위도 버튼패널/메인화면까지 확대)됨 — `manipulation`이 핀치줌은 오히려 명시적으로 허용한다는 걸 놓친 실수. 시도 6(`pan-x pan-y`로 교체, 핀치줌 권한 자체를 안 줌)으로 재수정, 배포 완료. 상세는 `docs/TROUBLESHOOTING.md` #19 참고.
- [ ] **기기별 미디어 쿼리 적용 (부분 완료)**: `@media (max-height: 750px)`로 관전 화면의 `SequenceBoard`/`.content` 크기를 줄여 짧은 세로 뷰포트에서 채팅창이 눌리던 문제는 고침(`docs/TROUBLESHOOTING.md` #15). 다만 이건 세로 길이 기준 대응 하나뿐 — 가로 폭이 매우 좁은 기기, `TeamRosterPanel`/`ButtonPanel` 등 다른 화면 요소들의 극단적 화면비는 아직 점검 안 함.

## 확인 필요 (명세 확정 대기)

- [ ] **시퀀스 조립 시 양쪽 역할 보장 여부**: 현재 구현(`server/src/game/sequence.ts`)은 매 스텝마다 돼지/토끼 조각 중 무작위로 골라 이어붙이는 방식이라, "이번 턴엔 내 색상이 한 번도 안 나온다"가 이론상 가능함. 이대로 둘지, "양쪽 최소 1개씩 보장" 규칙을 추가할지 확인 필요.

## 후순위 (완성도)

- [ ] 상단 헤더 장식 요소 — 마스코트 아이콘, **누적 성공 횟수**(4초 타이머 progress바와는 별개, `docs/REQUIREMENTS.md` §9/§12.1 참고 — 이건 범위 제외 확정된 메타 시스템). 4초 타이머 바(`TimerBar`)는 이미 구현됨.
