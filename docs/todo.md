# TODO

> 지금까지 한 작업은 git 커밋 로그와 `docs/REQUIREMENTS.md`/`docs/ARCHITECTURE.md`/`docs/TROUBLESHOOTING.md` 참고. 이 문서는 앞으로 할 일만 정리.

## 다음에 먼저 볼 것

- [ ] **iOS(Safari+Chrome, WebKit 공유)에서 버튼 입력이 씹히는 현상 — 기술적 드롭 가능성은 낮아짐, 사용자 확인 대기**: 안드로이드 문제(2026-07-15 이전 세션)는 `touch-action`/pointerdown+dedupe로 해결됐지만, 아이폰에서 재발 신고. 시도 1(뷰포트 `maximum-scale=1.0`/`user-scalable=no`로 핀치줌 차단) 효과 없음. 이후 화면에 이벤트 로그 오버레이(`ButtonPanel.tsx`, 검증 후 원복)를 붙여 실제 씹힘 재현 시점의 로그를 캡처한 결과 — **`pointerdown`/`touchstart`는 매 탭마다 정상 발생**(iOS 터치 이벤트 드롭이 원인이 아님), 캡처된 사례는 순서를 하나 틀린(다음 정답은 보라인데 주황을 누름) 실제 오답이었고, 오답 처리 후 절구 시스템 규칙대로 4초간 버튼이 정상적으로 잠긴 것 — 이게 "씹힘"으로 느껴졌을 가능성이 있음. 다만 이 로그는 버튼 자체에만 리스너가 달려있어 "터치가 버튼에 아예 안 찍히는" 케이스는 여전히 구분 불가능하므로 완전히 배제된 건 아님. **다음 단계**: 실패 배너(`TurnOutcomeBanner`)가 4초 잠김 상태를 충분히 명확하게 보여주는지 사용자 확인 대기 — 배너가 명확한데도 계속 "씹힌다"고 느껴지면 그때 후보 원인 (1) iOS 시스템 제스처 인식기의 터치 가로채기, (2) WebKit 연속 터치 처리 한계, (3) 리렌더링/터치 타이밍 겹침 을 `document` 레벨 좌표 로깅으로 재조사.
- [ ] **기기별 미디어 쿼리 적용**: 지금까지 개발/테스트는 480×800(모바일 기준 뷰포트) 고정으로만 확인함. 실제 기기별(화면 크기/가로세로 비율 다양한 안드로이드·아이폰)로 레이아웃이 깨지는 곳이 없는지 확인하고 필요한 곳에 미디어 쿼리 추가 필요.

## 확인 필요 (명세 확정 대기)

- [ ] **시퀀스 조립 시 양쪽 역할 보장 여부**: 현재 구현(`server/src/game/sequence.ts`)은 매 스텝마다 돼지/토끼 조각 중 무작위로 골라 이어붙이는 방식이라, "이번 턴엔 내 색상이 한 번도 안 나온다"가 이론상 가능함. 이대로 둘지, "양쪽 최소 1개씩 보장" 규칙을 추가할지 확인 필요.

## 후순위 (완성도)

- [ ] 상단 헤더 장식 요소 — 마스코트 아이콘, **누적 성공 횟수**(4초 타이머 progress바와는 별개, `docs/REQUIREMENTS.md` §9/§12.1 참고 — 이건 범위 제외 확정된 메타 시스템). 4초 타이머 바(`TimerBar`)는 이미 구현됨.
- [ ] 접속 끊김 관련 UI — 재접속 유예(60초) 중인 팀원이 있다는 걸 나머지 플레이어 화면에 보여줄지 (지금은 서버 로직만 있고 클라이언트에 반영 안 됨)
