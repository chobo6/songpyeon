# 유저 밴 기능 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 관리자 페이지에서 특정 유저를 밴/해제할 수 있게 한다. 밴 즉시 현재 접속 중인 방에서 강제 퇴장되고, 이후 온라인 매치 입장/생성이 막힌다.

**Architecture:** `users` 테이블에 `banned_at` 컬럼을 추가하고, `MatchRoom.onAuth`에서 이 값을 확인해 밴된 유저의 입장을 거절한다. 밴 API는 `setUserBanned`로 DB를 갱신한 뒤, `matchMaker.getLocalRoomById`로 이 프로세스에 떠 있는 모든 방의 실제 인스턴스를 찾아 새로 추가하는 `MatchRoom.kickUserId(userId)`를 호출해 현재 접속을 즉시 끊는다.

**Tech Stack:** Node/TypeScript, better-sqlite3, Colyseus, Express, React.

## Global Constraints

- **영구 밴만 지원** — 기간제(임시 밴)는 범위 밖.
- 밴은 **온라인 매치 입장/생성만** 차단한다. 로그인, 방 목록 열람은 막지 않는다.
- **밴하는 즉시 강제 퇴장** — 현재 어떤 방에 있든(플레이어든 관전자든) 그 방에서 바로 튕겨나간다.
- 밴 사유(reason) 입력 UI는 없음 — 단순 밴/해제 토글.
- 밴된 유저는 관리자 유저 목록에서 행이 시각적으로 구분(흐리게 표시)된다.

---

### Task 1: DB 마이그레이션 — `banned_at` 컬럼

**Files:**
- Modify: `server/src/db/connection.ts`
- Test: `server/src/db/connection.test.ts`

**Interfaces:**
- Consumes: 없음(이 태스크가 시작점).
- Produces: `users` 테이블에 `banned_at TEXT` 컬럼(NULL 허용, 기본값 NULL). 이후 태스크들이 `SELECT ... banned_at ...`, `UPDATE users SET banned_at = ...`로 이 컬럼을 직접 사용한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`server/src/db/connection.test.ts`의 `describe("createDb", ...)` 블록 안, 마지막 테스트(`"a pre-existing DB's UTC created_at values are shifted to KST exactly once"`) 뒤에 추가:

```ts
  test("users get a banned_at column that defaults to NULL", () => {
    const db = createDb(":memory:");
    db.prepare(`INSERT INTO users (google_sub) VALUES (?)`).run("sub-ban");

    const row = db.prepare(`SELECT banned_at FROM users WHERE google_sub = ?`).get("sub-ban") as {
      banned_at: string | null;
    };
    expect(row.banned_at).toBeNull();
  });
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test --workspace server -- connection.test.ts`
Expected: FAIL — `SqliteError: no such column: banned_at`

- [ ] **Step 3: 마이그레이션 구현**

`server/src/db/connection.ts` 전체를 아래로 교체:

```ts
import Database from "better-sqlite3";

export function createDb(filename: string): Database.Database {
  const db = new Database(filename);
  db.pragma("journal_mode = WAL");
  db.exec(`
    CREATE TABLE IF NOT EXISTS users (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      google_sub TEXT UNIQUE NOT NULL,
      email TEXT,
      name TEXT,
      nickname TEXT,
      max_round INTEGER NOT NULL DEFAULT 0,
      banned_at TEXT,
      created_at TEXT NOT NULL DEFAULT (datetime('now', '+9 hours'))
    )
  `);
  // SQLite's UNIQUE index treats each NULL as distinct from every other NULL,
  // so accounts that haven't set a nickname yet (nickname IS NULL) don't
  // collide with each other — only two non-null nicknames can't match.
  db.exec(`CREATE UNIQUE INDEX IF NOT EXISTS idx_users_nickname ON users(nickname)`);

  // CREATE TABLE IF NOT EXISTS only defines max_round/banned_at for a
  // brand-new database — it does nothing to the production DB file, which
  // already has a users table from before these columns existed. ALTER
  // TABLE ADD COLUMN is the migration for that case; guarded so re-running
  // it on an already-migrated DB (every subsequent server start) is a no-op
  // instead of an error.
  const columns = (db.prepare(`PRAGMA table_info(users)`).all() as { name: string }[]).map((col) => col.name);
  if (!columns.includes("max_round")) {
    db.exec(`ALTER TABLE users ADD COLUMN max_round INTEGER NOT NULL DEFAULT 0`);
  }
  if (!columns.includes("banned_at")) {
    db.exec(`ALTER TABLE users ADD COLUMN banned_at TEXT`);
  }

  // created_at used to default to UTC (datetime('now')); rows written before
  // this changed to KST (+9 hours, above and in getOrCreateUser's INSERT)
  // still hold UTC values. user_version gates the one-time shift so it never
  // re-runs on a later startup and drifts already-corrected rows forward
  // again — SQLite databases default to user_version 0 and nothing else in
  // this app touches that pragma.
  const schemaVersion = db.pragma("user_version", { simple: true }) as number;
  if (schemaVersion < 1) {
    db.exec(`UPDATE users SET created_at = datetime(created_at, '+9 hours')`);
    db.pragma("user_version = 1");
  }

  return db;
}

export const db = createDb(process.env.SQLITE_DB_PATH ?? "data/songpyeon.db");
```

(변경점: `CREATE TABLE`에 `banned_at TEXT` 추가, 컬럼 존재 확인을 `hasMaxRound` 단일 변수에서 `columns` 배열 재사용 방식으로 바꾸고 `banned_at`에 대한 동일한 가드를 추가.)

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test --workspace server -- connection.test.ts`
Expected: PASS (전체 5개 테스트)

- [ ] **Step 5: 커밋**

```bash
git add server/src/db/connection.ts server/src/db/connection.test.ts
git commit -m "유저 밴 기능을 위한 banned_at 컬럼 추가"
```

---

### Task 2: `googleAuth.ts` — 타입 확장 + `setUserBanned`

**Files:**
- Modify: `server/src/auth/googleAuth.ts`
- Test: `server/src/auth/googleAuth.test.ts`

**Interfaces:**
- Consumes: Task 1의 `banned_at` 컬럼.
- Produces:
  - `UserProfile = { id: number; nickname: string | null; bannedAt: string | null }` (기존 타입 확장)
  - `AdminUserRow`에 `bannedAt: string | null` 필드 추가
  - `setUserBanned(userId: number, banned: boolean): void` — Task 3(`MatchRoom.ts`)과 Task 4(`createServer.ts`)가 이 함수를 가져다 쓴다.
  - `getUserById(userId: number): UserProfile | undefined`가 이제 `bannedAt`도 채워서 반환 — Task 3의 `onAuth`가 `user.bannedAt`을 읽는다.

- [ ] **Step 1: 실패하는 테스트 작성**

`server/src/auth/googleAuth.test.ts` 맨 위 import를 아래로 교체(`setUserBanned` 추가):

```ts
import {
  adminSetNickname,
  getOrCreateUser,
  getTopRanking,
  getUserById,
  listUsers,
  recordRoundAchievement,
  setNickname,
  setUserBanned,
} from "./googleAuth";
```

파일 맨 끝(`describe("getTopRanking", ...)` 블록 뒤)에 추가:

```ts
describe("setUserBanned", () => {
  beforeEach(() => {
    db.exec("DELETE FROM users");
  });

  test("banning a user sets bannedAt, unbanning clears it", () => {
    const user = getOrCreateUser("sub-20", {});
    expect(getUserById(user.id)?.bannedAt).toBeNull();

    setUserBanned(user.id, true);
    expect(getUserById(user.id)?.bannedAt).toBeTruthy();

    setUserBanned(user.id, false);
    expect(getUserById(user.id)?.bannedAt).toBeNull();
  });

  test("listUsers reflects the banned status", () => {
    const user = getOrCreateUser("sub-21", {});
    setUserBanned(user.id, true);
    const row = listUsers().find((u) => u.id === user.id);
    expect(row?.bannedAt).toBeTruthy();
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test --workspace server -- googleAuth.test.ts`
Expected: FAIL — `setUserBanned` is not exported / TypeScript 컴파일 에러

- [ ] **Step 3: 구현**

`server/src/auth/googleAuth.ts`에서 아래 부분들을 교체.

`UserProfile` 타입 (파일 상단):

```ts
export type UserProfile = { id: number; nickname: string | null; bannedAt: string | null };
```

`getOrCreateUser`의 마지막 SELECT (함수 본문 마지막 줄):

```ts
  return db
    .prepare(`SELECT id, nickname, banned_at AS bannedAt FROM users WHERE google_sub = ?`)
    .get(googleSub) as UserProfile;
}
```

`getUserById` 전체:

```ts
export function getUserById(userId: number): UserProfile | undefined {
  return db
    .prepare(`SELECT id, nickname, banned_at AS bannedAt FROM users WHERE id = ?`)
    .get(userId) as UserProfile | undefined;
}
```

`AdminUserRow` 타입:

```ts
export type AdminUserRow = {
  id: number;
  email: string | null;
  name: string | null;
  nickname: string | null;
  bannedAt: string | null;
  createdAt: string;
};
```

`listUsers` 전체:

```ts
export function listUsers(): AdminUserRow[] {
  return db
    .prepare(
      `SELECT id, email, name, nickname, banned_at AS bannedAt, created_at AS createdAt FROM users ORDER BY id DESC`,
    )
    .all() as AdminUserRow[];
}
```

`adminSetNickname` 함수 바로 뒤에 새 함수 추가:

```ts
export function setUserBanned(userId: number, banned: boolean): void {
  if (banned) {
    db.prepare(`UPDATE users SET banned_at = datetime('now', '+9 hours') WHERE id = ?`).run(userId);
  } else {
    db.prepare(`UPDATE users SET banned_at = NULL WHERE id = ?`).run(userId);
  }
}
```

- [ ] **Step 4: 테스트 통과 확인 + 타입체크**

Run: `npm test --workspace server -- googleAuth.test.ts`
Expected: PASS (전체 테스트, 새로 추가한 2개 포함)

Run: `npm run build --workspace server`
Expected: 에러 없음 (타입체크만, 산출물 없음)

- [ ] **Step 5: 커밋**

```bash
git add server/src/auth/googleAuth.ts server/src/auth/googleAuth.test.ts
git commit -m "googleAuth에 setUserBanned 추가하고 UserProfile/AdminUserRow에 bannedAt 반영"
```

---

### Task 3: `MatchRoom.ts` — onAuth 차단 + `kickUserId`

**Files:**
- Modify: `server/src/rooms/MatchRoom.ts`
- Test: `server/src/rooms/MatchRoom.test.ts`

**Interfaces:**
- Consumes: Task 2의 `getUserById`(이미 import돼 있음, `bannedAt` 필드만 새로 씀), `setUserBanned`(테스트에서 밴 상태를 만들 때 사용).
- Produces: `MatchRoom.kickUserId(userId: number): boolean` — 이 방에 연결된 클라이언트 중 해당 userId를 찾아 강제 종료시키고 성공하면 `true`, 없으면 `false`를 반환. Task 4(`createServer.ts`)가 밴 라우트에서 이 메서드를 호출한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`server/src/rooms/MatchRoom.test.ts` 상단 import 수정 — `../auth/googleAuth`에서 가져오는 목록에 `setUserBanned` 추가:

```ts
import { getOrCreateUser, setNickname, setUserBanned } from "../auth/googleAuth";
```

`import type { Room as ServerRoom } from "colyseus";` 바로 아래 줄에 추가:

```ts
import { MatchRoom } from "./MatchRoom";
```

파일 맨 끝, `describe("admin event log integration", ...)` 블록이 끝나는 `});` 바로 뒤(외부 `describe("MatchRoom", ...)`가 닫히기 전)에 새 블록 추가:

```ts
  describe("user ban", () => {
    test("onAuth rejects a banned user's join attempt", async () => {
      const room = await colyseus.createRoom<MatchState>("match", { teamCount: 1 });
      testUserCounter += 1;
      const user = getOrCreateUser(`test-google-sub-${testUserCounter}`, {});
      setNickname(user.id, "밴유저");
      setUserBanned(user.id, true);
      const token = signSession(user.id);
      const port = (colyseus.server as unknown as { port: number }).port;
      const client = new ColyseusJsClient(`ws://127.0.0.1:${port}`, {
        headers: { Cookie: `session=${token}` },
      });

      await expect(client.joinById<MatchState>(room.roomId)).rejects.toThrow("이용이 제한된 계정입니다");
    });

    test("kickUserId force-disconnects the given user's client from the room", async () => {
      const room = await colyseus.createRoom<MatchState>("match", { teamCount: 1 });
      testUserCounter += 1;
      const user = getOrCreateUser(`test-google-sub-${testUserCounter}`, {});
      setNickname(user.id, "강퇴대상");
      const token = signSession(user.id);
      const port = (colyseus.server as unknown as { port: number }).port;
      const client = new ColyseusJsClient(`ws://127.0.0.1:${port}`, {
        headers: { Cookie: `session=${token}` },
      });
      const joinedRoom = await client.joinById<MatchState>(room.roomId);
      await flush();
      expect(room.state.players.has(joinedRoom.sessionId)).toBe(true);

      const kicked = (room as unknown as MatchRoom).kickUserId(user.id);
      await flush();

      expect(kicked).toBe(true);
      expect(room.state.players.has(joinedRoom.sessionId)).toBe(false);
    });

    test("kickUserId returns false when the user isn't connected to this room", async () => {
      const room = await colyseus.createRoom<MatchState>("match", { teamCount: 1 });
      expect((room as unknown as MatchRoom).kickUserId(999999)).toBe(false);
    });
  });
});
```

주의: 마지막의 `});`는 기존에 있던, 외부 `describe("MatchRoom", ...)`를 닫는 그 `});`다 — 새 `describe("user ban", ...)` 블록을 그 앞에 끼워 넣는 것이지, 닫는 괄호를 새로 추가하는 게 아니다.

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test --workspace server -- MatchRoom.test.ts`
Expected: FAIL — `kickUserId` is not a function / 밴된 유저의 join이 거절되지 않고 성공함

- [ ] **Step 3: 구현**

`server/src/rooms/MatchRoom.ts`의 `onAuth` 메서드를 아래로 교체:

```ts
  async onAuth(_client: Client, _options: MatchRoomOptions, context: AuthContext) {
    const token = getCookieValue(context.headers?.cookie, SESSION_COOKIE_NAME);
    const userId = verifySession(token);
    const user = userId ? getUserById(userId) : undefined;
    if (!user || !user.nickname) {
      throw new Error("로그인이 필요합니다.");
    }
    if (user.bannedAt) {
      throw new Error("이용이 제한된 계정입니다.");
    }
    return { ip: context.ip, userId: user.id, nickname: user.nickname };
  }
```

`onLeave` 메서드가 끝나는 `}` 바로 뒤, `private rosterForMetadata()` 바로 앞에 새 public 메서드 추가:

```ts
  // 관리자 밴 API에서 호출 — 이 방에 연결된 클라이언트 중 해당 유저를 찾아 서버
  // 쪽에서 강제로 연결을 끊는다. this.clients는 플레이어/관전자 구분 없이 이
  // 방에 연결된 모든 클라이언트를 담고 있고, client.auth는 onAuth가 반환한 값이
  // 역할과 무관하게 그대로 들어있으므로 이 하나로 양쪽 다 찾아진다. client.leave()
  // 호출은 정상적으로 이 방의 onLeave를 실행시켜(플레이어면 절구/로스터 정리,
  // 관전자면 즉시 제거) 기존 퇴장 로직을 그대로 태운다.
  kickUserId(userId: number): boolean {
    const client = this.clients.find((c) => c.auth?.userId === userId);
    if (!client) return false;
    client.leave();
    return true;
  }
```

- [ ] **Step 4: 테스트 통과 확인 + 타입체크**

Run: `npm test --workspace server -- MatchRoom.test.ts`
Expected: PASS (전체 테스트, 새로 추가한 3개 포함)

Run: `npm run build --workspace server`
Expected: 에러 없음

- [ ] **Step 5: 전체 서버 테스트 스위트 통과 확인**

Run: `npm test --workspace server`
Expected: PASS (전체 테스트 파일)

- [ ] **Step 6: 커밋**

```bash
git add server/src/rooms/MatchRoom.ts server/src/rooms/MatchRoom.test.ts
git commit -m "밴된 유저의 방 입장을 onAuth에서 거절하고, 강제 퇴장용 kickUserId 추가"
```

---

### Task 4: 관리자 라우트 — `/api/admin/users/:id/ban`, `/unban`

**Files:**
- Modify: `server/src/createServer.ts`

**Interfaces:**
- Consumes: Task 2의 `setUserBanned`, Task 3의 `MatchRoom.kickUserId`(이미 import된 `MatchRoom` 클래스에 캐스팅으로 접근).
- Produces: `POST /api/admin/users/:id/ban`, `POST /api/admin/users/:id/unban` — Task 5(`AdminUsers.tsx`)가 호출한다.

이 프로젝트의 `/api/admin/*` 라우트들은 자동 테스트가 없고 수동 curl 검증만 해왔다(`docs/todo.md` 참고 — 기존 관례). 이 태스크도 동일하게 구현 후 수동 검증으로 확인한다.

- [ ] **Step 1: import에 `setUserBanned` 추가**

`server/src/createServer.ts` 상단의 `./auth/googleAuth` import를 아래로 교체:

```ts
import {
  adminSetNickname,
  getOrCreateUser,
  getTopRanking,
  getUserById,
  listUsers,
  setNickname,
  setUserBanned,
  verifyGoogleIdToken,
} from "./auth/googleAuth";
```

- [ ] **Step 2: 라우트 추가**

`app.post("/api/admin/users/:id/nickname", ...)` 라우트가 끝나는 `});` 바로 뒤(그다음 `app.post("/api/admin/announce", ...)` 라우트 바로 앞)에 추가:

```ts
  // 밴 즉시 강제 퇴장까지 처리한다 — DB만 갱신하고 끝내면 이미 접속 중인
  // 세션은 다음 방 입장 시도 전까지 계속 게임을 할 수 있어 "즉시 퇴장"
  // 요구사항을 못 지킨다. getLocalRoomById는 이 프로세스에 떠 있는 실제 룸
  // 인스턴스를 반환한다(getRoomById와 달리 — 그건 룸 목록 캐시만 반환함).
  // 이 프로젝트는 단일 프로세스 배포라 "이 프로세스에 있는 것만"이 곧 전부다.
  app.post("/api/admin/users/:id/ban", requireAdmin, async (req, res) => {
    const userId = Number(req.params.id);
    if (!Number.isInteger(userId)) {
      res.status(400).json({ error: "invalid id" });
      return;
    }
    setUserBanned(userId, true);

    const rooms = await matchMaker.query({ name: "match" });
    for (const r of rooms) {
      const room = matchMaker.getLocalRoomById(r.roomId) as MatchRoom | undefined;
      room?.kickUserId(userId);
    }

    res.json({ ok: true });
  });

  app.post("/api/admin/users/:id/unban", requireAdmin, async (req, res) => {
    const userId = Number(req.params.id);
    if (!Number.isInteger(userId)) {
      res.status(400).json({ error: "invalid id" });
      return;
    }
    setUserBanned(userId, false);
    res.json({ ok: true });
  });
```

- [ ] **Step 3: 타입체크**

Run: `npm run build --workspace server`
Expected: 에러 없음

- [ ] **Step 4: 서버 기동 후 수동 curl 검증**

`server/.env`에 `ADMIN_PASSWORD`가 설정돼 있어야 한다(CLAUDE.md 참고). 터미널 하나에서 `npm run dev:server` 실행 후, 다른 터미널에서:

```bash
# 관리자 로그인 (쿠키 저장)
curl -i -c /tmp/admin-cookies.txt -X POST http://localhost:2567/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{"password":"<server/.env의 ADMIN_PASSWORD 값>"}'
```
Expected: `HTTP/1.1 200`, `{"ok":true}`

```bash
# 밴 전 유저 목록에서 아무 id나 하나 확인
curl -s -b /tmp/admin-cookies.txt http://localhost:2567/api/admin/users
```
Expected: JSON 배열, 각 항목에 `"bannedAt":null` 포함

```bash
# 그 id로 밴 (아래 <id>를 실제 값으로 교체)
curl -i -b /tmp/admin-cookies.txt -X POST http://localhost:2567/api/admin/users/<id>/ban
```
Expected: `HTTP/1.1 200`, `{"ok":true}`

```bash
# 다시 목록 조회 — bannedAt이 시각 문자열로 채워졌는지 확인
curl -s -b /tmp/admin-cookies.txt http://localhost:2567/api/admin/users
```
Expected: 해당 id의 `"bannedAt"`이 더 이상 null이 아님

```bash
# 밴 해제
curl -i -b /tmp/admin-cookies.txt -X POST http://localhost:2567/api/admin/users/<id>/unban
```
Expected: `HTTP/1.1 200`, `{"ok":true}`, 이후 목록 조회 시 다시 `"bannedAt":null`

강제 퇴장까지 확인하려면: 브라우저로 그 유저 계정으로 로그인해 아무 방에나 들어간 채로 위 ban curl을 날려보고, 브라우저 쪽 연결이 즉시 끊기는지(방 목록/로그인 화면 등으로 튕기는지) 확인.

- [ ] **Step 5: 커밋**

```bash
git add server/src/createServer.ts
git commit -m "관리자 밴/밴 해제 라우트 추가 (밴 시 강제 퇴장까지 처리)"
```

---

### Task 5: `AdminUsers.tsx` — 밴/해제 UI

**Files:**
- Modify: `client/src/components/AdminUsers.tsx`
- Modify: `client/src/components/AdminUsers.module.css`

**Interfaces:**
- Consumes: Task 4의 `POST /api/admin/users/:id/ban`, `POST /api/admin/users/:id/unban`.
- Produces: 없음 (UI 최종 단계).

이 프로젝트의 client 워크스페이스는 테스트 프레임워크가 없다(`npm run build`/`npm run lint`가 검증 수단). 브라우저 확인은 `npm run sync-public` 후 `http://localhost:2567/admin`에서 가능하면 하되, 필수 단계는 아니다.

- [ ] **Step 1: `UserRow` 타입에 `bannedAt` 추가**

`client/src/components/AdminUsers.tsx`의 `UserRow` 타입을 아래로 교체:

```ts
type UserRow = {
  id: number;
  email: string | null;
  name: string | null;
  nickname: string | null;
  bannedAt: string | null;
  createdAt: string;
};
```

- [ ] **Step 2: 밴 처리 상태 + 함수 추가**

`const [saving, setSaving] = useState(false);` 바로 뒤에 추가:

```ts
  const [banningId, setBanningId] = useState<number | null>(null);
```

`saveEdit` 함수가 끝나는 `}` 바로 뒤에 새 함수 추가:

```ts
  async function toggleBan(user: UserRow) {
    setBanningId(user.id);
    setError(null);
    try {
      const endpoint = user.bannedAt ? "unban" : "ban";
      const res = await fetch(`/api/admin/users/${user.id}/${endpoint}`, {
        method: "POST",
        credentials: "same-origin",
      });
      if (!res.ok) {
        if (res.status === 401) {
          onUnauthorized();
          return;
        }
        setError("처리에 실패했습니다");
        return;
      }
      await loadUsers();
    } catch {
      setError("처리에 실패했습니다");
    } finally {
      setBanningId(null);
    }
  }
```

- [ ] **Step 3: 행 렌더링에 밴 표시 + 버튼 추가**

`{users.map((user) => (` 로 시작하는 `<tr key={user.id}>`를 아래로 교체:

```tsx
                <tr key={user.id} className={user.bannedAt ? styles.bannedRow : undefined}>
```

액션 셀의 else 분기(편집 중이 아닐 때 "수정" 버튼만 있던 부분)를 아래로 교체:

```tsx
                    ) : (
                      <>
                        <button className={styles.smallButton} onClick={() => startEdit(user)}>
                          수정
                        </button>
                        <button
                          className={styles.smallButton}
                          onClick={() => toggleBan(user)}
                          disabled={banningId === user.id}
                        >
                          {user.bannedAt ? "밴 해제" : "밴"}
                        </button>
                      </>
                    )}
```

- [ ] **Step 4: CSS에 밴된 행 스타일 추가**

`client/src/components/AdminUsers.module.css` 맨 끝(`.smallButton:disabled` 규칙 뒤)에 추가:

```css

.bannedRow {
  opacity: 0.5;
}
```

- [ ] **Step 5: 타입체크 + lint**

Run: `npm run build --workspace client`
Expected: 에러 없음

Run: `npm run lint --workspace client`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add client/src/components/AdminUsers.tsx client/src/components/AdminUsers.module.css
git commit -m "관리자 유저 목록에 밴/해제 토글 버튼과 밴된 행 시각적 구분 추가"
```
